
export const bgColor = (isDarkMode : boolean) => {
    return isDarkMode ? "bg-[#00000080] " : "bg-[#ffffff80] ";
}
export const textColor = (isDarkMode : boolean) => {
    return isDarkMode ? "white" : "black";
}